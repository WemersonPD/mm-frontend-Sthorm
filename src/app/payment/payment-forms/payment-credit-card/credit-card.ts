export interface CreditCard {
  cardNumber: string;
  holder: string;
  monthExpiration: string;
  yearExpiration: string;
  securityCode: string;
  cpf: string;
  email: string;
  phoneNumber: string;
  brand: string;
}
